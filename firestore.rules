rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access to the database.
    // The backend uses the Admin SDK which bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
